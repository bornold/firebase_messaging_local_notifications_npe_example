name: firebase_messaging_local_notifications_npe_example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: ^7.0.0
  flutter_local_notifications:
    git:
      url: git://github.com/MaikuB/flutter_local_notifications.git
      ref: time_zone_macos_support
      path: flutter_local_notifications/

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
